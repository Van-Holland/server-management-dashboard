services:
  server-dashboard:
    build: .
    container_name: server-dashboard
    restart: unless-stopped
    ports:
      - "3005:3000"
    volumes:
      - /:/host/root:ro
      - /mnt/storage8tb:/host/storage8tb:ro
      - /proc:/host/proc:ro
